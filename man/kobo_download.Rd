% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_management.R
\name{kobo_download}
\alias{kobo_download}
\title{Download submissions from KoboToolbox}
\usage{
kobo_download(
  url,
  username,
  password,
  assetid = NULL,
  name = NULL,
  language = "en"
)
}
\arguments{
\item{url}{Character. KoboToolbox server URL (e.g., \code{"eu.kobotoolbox.org"}).}

\item{username}{Character. KoboToolbox username for authentication.}

\item{password}{Character. KoboToolbox password for authentication.}

\item{assetid}{Character. Kobo asset UID. Optional if \code{name} is provided.}

\item{name}{Character. Form (questionnaire) name used to retrieve \code{assetid}
if not supplied.}

\item{language}{Character. Language code used for messages. Default is \code{"en"}.}
}
\value{
A list containing all submission records retrieved from the KoboToolbox API.
}
\description{
Downloads all submissions (data records) from a KoboToolbox form (asset)
using the KoboToolbox v2 API. The function handles pagination automatically
and returns a complete list of submissions.
}
\details{
If \code{assetid} is not provided, it is retrieved using the form \code{name}.

The function performs the following steps:
\enumerate{
\item Validates required inputs (\code{assetid} or \code{name} must be provided).
\item Retrieves \code{assetid} from the Kobo API if only \code{name} is given.
\item Sends GET requests to the KoboToolbox \verb{/api/v2/assets/\{assetid\}/data/} endpoint.
\item Automatically follows pagination via the \code{next} field.
\item Aggregates all submission records into a single list.
}

On success, a message is printed indicating the name of submissions downloaded.

Common HTTP error codes:
\itemize{
\item \strong{401 / 403}: Unauthorized access (invalid credentials or permissions)
\item \strong{404}: Asset (form) not found
}
}
\section{Dependencies}{

This function relies on:
\itemize{
\item \code{kobo_api()} to retrieve asset metadata when \code{name} is used
\item \code{MSSG()} for multilingual messages
\item \code{httr} for HTTP requests
}
}

\examples{
\dontrun{
# Download by asset ID
data <- kobo_download(
  url = "eu.kobotoolbox.org",
  username = "user",
  password = "pass",
  assetid = "aBc123"
)

# Download by form name
data <- kobo_download(
  url = "eu.kobotoolbox.org",
  username = "user",
  password = "pass",
  name = "My Form"
)
}

}
