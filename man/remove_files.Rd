% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_files.R
\name{remove_files}
\alias{remove_files}
\title{Remove files or directories with retry and timeout}
\usage{
remove_files(path, recursive = TRUE, timeout = 60, wait = 0.5)
}
\arguments{
\item{path}{A character vector of file or directory paths to remove.}

\item{recursive}{Logical; whether to delete directories recursively.
Defaults to \code{TRUE}.}

\item{timeout}{Numeric; maximum number of seconds to keep retrying
deletion before giving up. Defaults to \code{60}.}

\item{wait}{Numeric; number of seconds to wait between retry attempts.
Defaults to \code{0.5}.}
}
\value{
Invisibly returns \code{TRUE} if all paths are successfully removed.
Returns \code{FALSE} if the operation times out before completion.
}
\description{
This function attempts to remove one or more files or directories,
retrying until successful or until a timeout is reached. It is useful
in situations where files may be temporarily locked (e.g., open
connections), as it repeatedly tries deletion while closing open
connections between attempts.
}
\details{
For directories, files and subdirectories are removed recursively
(depending on the \code{recursive} argument).

The function:
\itemize{
\item Repeatedly attempts to delete each path until successful.
\item Calls \code{closeAllConnections()} between attempts to release file locks.
\item Prints progress to the console using \code{"."} for each retry cycle.
\item Stops when all paths are removed or when \code{timeout} is exceeded.
}

Non-existent paths are silently ignored.
}
\examples{
\dontrun{
# Remove a file
remove_files("temp.txt")

# Remove multiple paths
remove_files(c("file1.txt", "dir1"))
}

}
