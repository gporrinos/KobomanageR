% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_write_read.R
\name{readDAT}
\alias{readDAT}
\title{Read a data frame from CSV or Excel file}
\usage{
readDAT(sheetname = NULL, filepath, fileformat, csv_separator = NULL)
}
\arguments{
\item{sheetname}{A character string specifying the worksheet name when
reading Excel files. Required if \code{fileformat = "xlsx"}.}

\item{filepath}{A character string specifying the file path to read from.}

\item{fileformat}{A character string specifying the input format.
Supported values are \code{"csv"} and \code{"xlsx"}.}

\item{csv_separator}{Optional character string specifying the CSV separator.
Must be either \code{","} (comma) or \code{";"} (semicolon). If \code{NULL}, the separator
is determined based on the system locale.}
}
\value{
A data frame containing the imported data.
}
\description{
This function imports data from a CSV or Excel file and returns it as a
data frame. For CSV files, the separator can be specified manually or
inferred from the system locale. For Excel files, a specific worksheet
must be provided.
}
\details{
For CSV files:
\itemize{
\item If \code{csv_separator} is provided, it determines whether \code{utils::read.csv()}
or \code{utils::read.csv2()} is used.
\item If \code{csv_separator} is \code{NULL}, the system decimal separator is used
to infer the appropriate format.
}

For Excel files:
\itemize{
\item Data is imported using \code{readxl::read_excel()}.
\item The result is converted to a data frame.
\item \code{guess_max} is set to 100000 to improve type inference.
}
}
\examples{
\dontrun{
df <- readDAT(filepath = "mtcars.csv", fileformat = "csv")
df <- readDAT(filepath = "mtcars_semicolon.csv", fileformat = "csv", csv_separator = ";")
df <- readDAT(filepath = "mtcars.xlsx", fileformat = "xlsx", sheetname = "Sheet1")
}

}
