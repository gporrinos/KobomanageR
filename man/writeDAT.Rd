% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_write_read.R
\name{writeDAT}
\alias{writeDAT}
\title{Write a data frame to CSV or Excel file}
\usage{
writeDAT(data, fileformat, filepath, csv_separator = NULL, sheetname = NULL)
}
\arguments{
\item{data}{A data frame to be written to file.}

\item{fileformat}{A character string specifying the output format.
Supported values are \code{"csv"} and \code{"xlsx"}.}

\item{filepath}{A character string specifying the file path to write to.}

\item{csv_separator}{Optional character string specifying the CSV separator.
Must be either \code{","} (comma) or \code{";"} (semicolon). If \code{NULL}, the separator
is determined based on the system locale.}

\item{sheetname}{A character string specifying the worksheet name when writing
Excel files. Must be provided if \code{fileformat = "xlsx"}.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
This function writes a data frame to disk in either CSV or Excel format.
For CSV files, the field separator can be specified manually or inferred
from the system locale. For Excel files, the data is written to a single
worksheet.
}
\details{
For CSV output:
\itemize{
\item If \code{csv_separator} is provided, it determines whether \code{utils::write.csv()}
or \code{utils::write.csv2()} is used.
\item If \code{csv_separator} is \code{NULL}, the system decimal separator is used
to infer the appropriate CSV format.
}

For Excel output:
\itemize{
\item The data is written as a single worksheet using
\code{writexl::write_xlsx()}.
}
}
