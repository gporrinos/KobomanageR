% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kobo_submissions.R
\name{kobo_delete_submission}
\alias{kobo_delete_submission}
\title{Delete KoboToolbox submissions}
\usage{
kobo_delete_submission(
  url = "eu.kobotoolbox.org",
  username,
  password,
  assetid = NULL,
  name = NULL,
  submission_id = NULL,
  submission_uuid = NULL,
  language = "en"
)
}
\arguments{
\item{url}{Character. KoboToolbox server URL. Defaults to
\code{"eu.kobotoolbox.org"}.}

\item{username}{Character. KoboToolbox username for authentication.}

\item{password}{Character. KoboToolbox password for authentication.}

\item{assetid}{Character. KoboToolbox asset UID. Optional if \code{name} is provided.}

\item{name}{Character. Name of the form (questionnaire). Used to retrieve
\code{assetid} if not supplied.}

\item{submission_id}{Character vector. One or more submission \verb{_id} values.}

\item{submission_uuid}{Character vector. One or more submission \verb{_uuid} values.
Used only if \code{submission_id} is not provided.}

\item{language}{Character. Language code used for status and error messages.
Default is \code{"en"}.}
}
\value{
This function does not return a value. It prints status messages
indicating the result of each deletion.
}
\description{
Deletes one or more submissions from a KoboToolbox form (asset).
Submissions can be specified either by their internal submission IDs
(\verb{_id}) or by their UUIDs (\verb{_uuid}).
}
\details{
If \code{assetid} is not provided, it will be retrieved using the form
\code{name}. If \code{submission_id} is not provided, it will be derived from
the provided \code{submission_uuid}.

The function performs the following steps:
\enumerate{
\item Validates required inputs.
\item Retrieves the asset ID if only the form name is provided.
\item Converts submission UUIDs to submission IDs if needed.
\item Sends a DELETE request for each submission via the KoboToolbox API.
\item Displays status messages for each deletion attempt.
}

Successful deletions typically return HTTP status \verb{204 No Content}.
}
\section{Dependencies}{

This function relies on the following helper functions:
\itemize{
\item \code{kobo_projects()} to retrieve asset metadata
\item \code{kobo_download()} to fetch submissions
\item \code{kobotools_messages()} for multilingual messages
\item \code{coalesce_to()} for safe value extraction
}
}

